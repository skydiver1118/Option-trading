name: SOXL Daily TCAR No QQQ 10Y
on:
  workflow_dispatch:
  push:
    paths: ['scripts/soxl_daily_tcar_no_qqq_10y.py']
permissions:
  contents: write
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install pandas numpy requests
      - name: Run 10y backtest
        env:
          TRADIER_TOKEN: ${{ secrets.TRADIER_TOKEN }}
        run: python scripts/soxl_daily_tcar_no_qqq_10y.py
      - name: Commit results
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add data/williams_r/soxl_daily_tcar_no_qqq_10y_*.csv
          git commit -m 'Update 10y TCAR no QQQ results' || exit 0
          git push
